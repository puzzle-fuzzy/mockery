MOCKERY_API_PORT=3001
DATABASE_URL=postgres://mockery:mockery@localhost:5432/mockery
MOCKERY_STORAGE_ROOT=./storage
VITE_MOCKERY_API_BASE_URL=http://127.0.0.1:3001

# SMTP (optional; if unset, invitation emails are logged to the console instead of sent)
# MOCKERY_SMTP_HOST=smtp.example.com
# MOCKERY_SMTP_PORT=587
# MOCKERY_SMTP_USER=noreply@example.com
# MOCKERY_SMTP_PASS=your-password
# MOCKERY_SMTP_FROM=Mockery <noreply@example.com>

# MOCKERY_APP_URL=http://localhost:5173
